services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Controller\MainController:
        autowire: true
        autoconfigure: true
        public: true
        arguments:
            $session: '@session'

#    # Explicit service definition for MainController
#    App\Controller\MainController:
#        arguments:
#            $session: '@session'
#
#    # Explicit service definition for MultiPlayerRepository & SinglePlayerRepository
#    App\Service\MultiPlayerRepository:
#        arguments:
#            $session: '@session'
#
#
#    App\Service\SinglePlayerRepository:
#        arguments:
#            $session: '@session'
